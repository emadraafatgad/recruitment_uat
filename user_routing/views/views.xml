<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="user_routing.list">
      <field name="name">user_routing list</field>
      <field name="model">user_routing.user_routing</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="user_routing.action_window">
      <field name="name">user_routing window</field>
      <field name="res_model">user_routing.user_routing</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="user_routing.action_server">
      <field name="name">user_routing server</field>
      <field name="model_id" ref="model_user_routing_user_routing"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="user_routing" id="user_routing.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="user_routing.menu_1" parent="user_routing.menu_root"/>
    <menuitem name="Menu 2" id="user_routing.menu_2" parent="user_routing.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="user_routing.menu_1_list" parent="user_routing.menu_1"
              action="user_routing.action_window"/>
    <menuitem name="Server to list" id="user_routing" parent="user_routing.menu_2"
              action="user_routing.action_server"/>
    -->
  </data>
</odoo>