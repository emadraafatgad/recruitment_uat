<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="laborer_claims.list">
      <field name="name">laborer_claims list</field>
      <field name="model">laborer_claims.laborer_claims</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="laborer_claims.action_window">
      <field name="name">laborer_claims window</field>
      <field name="res_model">laborer_claims.laborer_claims</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="laborer_claims.action_server">
      <field name="name">laborer_claims server</field>
      <field name="model_id" ref="model_laborer_claims_laborer_claims"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="laborer_claims" id="laborer_claims.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="laborer_claims.menu_1" parent="laborer_claims.menu_root"/>
    <menuitem name="Menu 2" id="laborer_claims.menu_2" parent="laborer_claims.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="laborer_claims.menu_1_list" parent="laborer_claims.menu_1"
              action="laborer_claims.action_window"/>
    <menuitem name="Server to list" id="laborer_claims" parent="laborer_claims.menu_2"
              action="laborer_claims.action_server"/>
    -->
  </data>
</odoo>